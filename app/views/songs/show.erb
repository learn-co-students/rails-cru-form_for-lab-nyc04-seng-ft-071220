<h1> <%= @song.name %> </h1>

<ul>
    <li><%= link_to @song.artist.name, artist_path(@song.artist) if @song.artist %></li>
    <li><%= link_to @song.genre.name, genre_path(@song.genre) if @song.genre %></li>
   <li> <%= link_to 'Back to Songs Index', songs_path%></li>
</ul>